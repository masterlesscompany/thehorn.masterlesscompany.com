source "https://rubygems.org"

# 테마·플러그인 없이 코어만 쓴다 — 레이아웃과 CSS 는 전부 저장소 안에 있다.
gem "jekyll", "~> 4.4.1"

platforms :mingw, :x64_mingw, :mswin, :jruby do
  gem "tzinfo", ">= 1", "< 3"
  gem "tzinfo-data"
end
