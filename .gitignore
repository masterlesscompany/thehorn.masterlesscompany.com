# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/

# Node
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# 환경 변수 · 자격증명
.env
.env.*
!.env.example

# 서비스 계정 키. 커밋되면 API 키 유출보다 나쁘다.
gcp-sa.json
*-sa.json
*service-account*.json

# 런 기록 (로컬 플레이 산출물). 트레이스 샘플은 docs/trace-samples/ 에 따로 둔다.
backend/runs/
backend/eval/out/

# OS
.DS_Store

# Jekyll
_site/
.jekyll-cache/
.jekyll-metadata

# 개발일지 산출물 — scripts/split_devlog.py 가 devlog/devlog.md 에서 빌드 직전에 만든다
jekyll/_logs/*-team.md
jekyll/_includes/devlog_head.md
jekyll/_includes/devlog_extra.md

